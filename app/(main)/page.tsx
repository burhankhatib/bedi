import { HomePageNew } from './HomePageNew'

export const dynamic = 'force-dynamic'

/**
 * Homepage: DoorDash-style discovery with hero banners, categories, and location selection.
 * All users (logged in or not) can browse. /join is for tenants and drivers to sign up.
 */
export default function HomePage() {
  return <HomePageNew />
}
