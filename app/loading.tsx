import { HomePageSkeleton } from '@/components/loading'

/**
 * Root loading: initial app load.
 * Uses skeleton to show structure while loading.
 */
export default function Loading() {
  return <HomePageSkeleton />
}